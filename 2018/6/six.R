example_input <- read.csv('~/src/adventofcode/6/example_input.txt', header = FALSE, col.names = c('x','y'))
input <- read.csv('~/src/adventofcode/6/input.txt', header = F, col.names = c('x','y'))

example_zone <- matrix(nrow = 30, ncol = 30)
zone <- matrix(nrow = 400, ncol = 400)

for(i in 1:nrow(example_zone)) {
  for(j in 1:ncol(example_zone)) {
    for()
  }
}